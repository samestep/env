### Flit

Install [from conda-forge][conda-forge]:
```
$ conda install -c conda-forge flit
```
Then extend the `PATH` as necessary:
```
$ echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
```


[conda-forge]: https://anaconda.org/conda-forge/flit
